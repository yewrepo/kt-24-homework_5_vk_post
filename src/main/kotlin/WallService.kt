import post.Post

class WallService {

    private var lastId: Int = 0
    private var postArray: Array<Post> = arrayOf()

    fun add(post: Post): Post {
        TODO()
    }

    fun update(post: Post): Boolean {
        TODO()
    }
}